import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#101828",
      light: "#667085",
    },
    secondary: {
      main: "#344054",
      light: "#808080",
    },
  },
});

export default theme;
